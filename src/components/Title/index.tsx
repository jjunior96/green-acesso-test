import styled from 'styled-components';

const Title = styled.h1`
  font-weight: bold;
  font-size: 2.8rem;
  max-width: 680px;

  strong,
  span {
    font-weight: bold;
  }
`;

export default Title;
