import { Story, Meta } from '@storybook/react/types-6-0';
import ActionBar from '.';

export default {
  title: 'ActionBar',
  component: ActionBar
} as Meta;

export const Default: Story = () => <ActionBar />;
